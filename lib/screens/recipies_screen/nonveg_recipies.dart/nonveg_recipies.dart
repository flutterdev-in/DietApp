import 'package:flutter/material.dart';

class NonVegRecipies extends StatelessWidget {
  const NonVegRecipies({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("NonVeg Recipies"),
      ),
    );
  }
}
